# Solana RPC endpoint
SOLANA_RPC_ENDPOINT=https://api.devnet.solana.com

# Server port
PORT=3000

# Base path for URLs (if behind a reverse proxy like /iq)
BASE_PATH=

# Default creator address for NFT metadata (optional)
DEFAULT_CREATOR=
